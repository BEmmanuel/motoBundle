<h1>Vehicule creation</h1>

<form action="{{ path('vehicule_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('vehicule') }}">
            Back to the list
        </a>
    </li>
</ul>
